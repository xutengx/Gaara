<?php

/**
 * 本配置将直接赋值给 new chillerlan\QRCode\Output\QRImageOptions 对象
 * 更改 : 你可以在任何地方重新定义这些属性, 因为,他们都是 public, 在你不定义的时候这些就是缺省值
 * 新增 : 依照 chillerlan\QRCode\Output\QRImageOptions 的实例, 你可以加入任何有效属性在此
 */
return [
    /*
      |--------------------------------------------------------------------------
      | 图片类型
      |--------------------------------------------------------------------------
      |
      | Supported: "png", "jpg", "gif"
      |
     */
    'type' => 'png',
    
    /*
      |--------------------------------------------------------------------------
      | 图片存储路径以及文件名后缀, null则返回内容
      |--------------------------------------------------------------------------
      | optional cache file path (eg:/mnt/qrcode.png), null returns the image data
      |
      | Supported: null, string
      |
     */
    'cachefile' => null,
    
    /*
      |--------------------------------------------------------------------------
      | 是否返回base64格式, 否则返回二进制
      |--------------------------------------------------------------------------
      | wether to return the image data as base64 
      | or raw like from file_get_contents()
      |
      | Supported: true, false
      |
     */
    'base64' => true,
    
    /*
      |--------------------------------------------------------------------------
      | 图片像素大小
      |--------------------------------------------------------------------------
      | size of a QR code pixel (25 is HUGE!)
      |
      | Supported: 1 ... 25
      |
     */
    'pixelSize' => 5,
    
    /*
      |--------------------------------------------------------------------------
      | 
      |--------------------------------------------------------------------------
      | margin around the QR code
      |
      | Supported: 0 ... 25
      |
     */
    'marginSize' => 5,
    
    /*
      |--------------------------------------------------------------------------
      | 是否透明 ( 不支持jpeg )
      |--------------------------------------------------------------------------
      | toggle transparency (no jpeg support)
      |
      | Supported: true, false
      |
     */
    'transparent' => true,
    
    /*
      |--------------------------------------------------------------------------
      | 前景颜色(红)
      |--------------------------------------------------------------------------
      | foreground red
      |
      | Supported: 0 ... 255
      |
     */
    'fgRed' => 0,
    
    /*
      |--------------------------------------------------------------------------
      | 前景颜色(绿)
      |--------------------------------------------------------------------------
      | foreground green
      |
      | Supported: 0 ... 255
      |
     */
    'fgGreen' => 0,
    
    /*
      |--------------------------------------------------------------------------
      | 前景颜色(蓝)
      |--------------------------------------------------------------------------
      | foreground blue
      |
      | Supported: 0 ... 255
      |
     */
    'fgBlue' => 0,
    
    /*
      |--------------------------------------------------------------------------
      | 背景颜色(红)
      |--------------------------------------------------------------------------
      | background red
      |
      | Supported: 0 ... 255
      |
     */
    'bgRed' => 255,
    
    /*
      |--------------------------------------------------------------------------
      | 背景颜色(绿)
      |--------------------------------------------------------------------------
      | background green
      |
      | Supported: 0 ... 255
      |
     */
    'bgGreen' => 255,
    
    /*
      |--------------------------------------------------------------------------
      | 背景颜色(蓝)
      |--------------------------------------------------------------------------
      | background blue
      |
      | Supported: 0 ... 255
      |
     */
    'bgBlue' => 255,
    
    /*
      |--------------------------------------------------------------------------
      | 
      |--------------------------------------------------------------------------
      | imagepng() compression level, -1 = auto
      |
      | Supported: -1 ... 9
      |
     */
    'pngCompression' => -1,
    
    /*
      |--------------------------------------------------------------------------
      | 
      |--------------------------------------------------------------------------
      | imagejpeg() quality
      |
      | Supported: 0 - 100
      |
     */
    'jpegQuality' => 85,
];
